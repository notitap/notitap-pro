import { mediaResizeTiptapMediaResize } from './media-resize-tiptap-media-resize';

describe('mediaResizeTiptapMediaResize', () => {
  it('should work', () => {
    expect(mediaResizeTiptapMediaResize()).toEqual(
      'media-resize-tiptap-media-resize'
    );
  });
});
