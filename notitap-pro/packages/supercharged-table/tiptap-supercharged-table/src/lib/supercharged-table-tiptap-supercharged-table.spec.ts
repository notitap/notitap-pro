import { superchargedTableTiptapSuperchargedTable } from './supercharged-table-tiptap-supercharged-table';

describe('superchargedTableTiptapSuperchargedTable', () => {
  it('should work', () => {
    expect(superchargedTableTiptapSuperchargedTable()).toEqual(
      'supercharged-table-tiptap-supercharged-table'
    );
  });
});
